apiVersion: v1
kind: Service
metadata:
  name: {{ include "beeant-webapp.fullname" . }}
  labels:
    {{- include "beeant-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beeant-webapp.selectorLabels" . | nindent 4 }}
